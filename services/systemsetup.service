[Unit]
Description=System setup on user login
After=graphical-session.target

[Service]
Type=oneshot
ExecStart=/usr/bin/blackbox-terminal -c 'bash /etc/firstsetup/bin/setup'

[Install]
WantedBy=graphical-session.target
